name: My First Workflow
on:
  pull_request:
    branches:
    - main
  workflow_dispatch:


jobs:
  run-stuff:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/container-action@main
      with: 
        who-to-greet: 'Jesse'
