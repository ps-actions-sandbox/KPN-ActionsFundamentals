name: My First Workflow
on:
  pull_request:
    branches:
    - main
  workflow_dispatch:


jobs:
  run-stuff:
    runs-on: ubuntu-latest
    steps:
    - uses: ps-actions-sandbox/kpn-test@main
      with: 
        your-name: 'Jesse'
      id: kpntest

    - run: echo ${{ steps.kpntest.time }}
